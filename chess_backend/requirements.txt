Django>=5.0,<6.1
channels>=4.0
daphne>=4.0
django-cors-headers>=4.0
chess>=1.10
whitenoise>=6.6
dj-database-url>=2.1
channels-redis>=4.0   # only used if REDIS_URL is set (multi-worker deployments)
psycopg2-binary>=2.9  # only used if DATABASE_URL points at Postgres
